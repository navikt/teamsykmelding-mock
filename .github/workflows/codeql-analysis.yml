name: CodeQL
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '0 0 * * 3'
jobs:
  codeql-jar-app-analysis:
    uses: navikt/teamsykmelding-github-actions-workflows/.github/workflows/codeql-jar-app-analysis.yml@main
    secrets: inherit